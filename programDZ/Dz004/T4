#include <stdio.h>
#include <stdlib.h>

int main()
{
    int s;
    int n;
    printf("Ведите число: ");
    scanf(" %d\n", &n);
    printf("Ведите число: ");
    scanf(" %d\n", &s);
    for(int a,k; a=s; k=n*a ++i)
        {
            printf("$d*$d=%d\n", n*s=k);
        }
    return 0;
}
